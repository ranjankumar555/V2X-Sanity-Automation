# Python dependencies for V2X sanity automation

# Serial communication (used as: import serial)
pyserial==3.5

# Excel handling for reports (DLT verification)
openpyxl>=3.1,<4.0

# SSH handling (used in ExecuteBySSH modules)
paramiko>=3.4,<4.0